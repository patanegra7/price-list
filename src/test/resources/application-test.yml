spring:
  # Database Properties
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=MySQL;INIT=CREATE SCHEMA IF NOT EXISTS public;DATABASE_TO_UPPER=false
 
# Logging
logging:
  level:
    dme.ecommerce.prices: debug
    org.springframework.web: debug
